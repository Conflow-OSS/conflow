{
  "name": "content-engine",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @content-engine/server",
    "typecheck": "npm run typecheck -w @content-engine/server",
    "test": "npm test -w @content-engine/server",
    "dev": "npm run dev -w @content-engine/server",
    "api:dev": "npm run api:dev -w @content-engine/server",
    "worker:dev": "npm run worker:dev -w @content-engine/server"
  }
}
